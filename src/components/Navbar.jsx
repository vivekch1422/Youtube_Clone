import { AppBar, Toolbar, Typography } from '@mui/material';

const Navbar = () => (
  <AppBar position="static">
    <Toolbar>
      <Typography variant="h6">MyTube</Typography>
    </Toolbar>
  </AppBar>
);

export default Navbar;
